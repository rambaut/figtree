<?xml version="1.0" ?>

<project name="FigTree" default="dist" basedir=".">
    <description>
        Build file for FigTree
        $Id:$
    </description>

    <!-- set global properties for this build -->
    <property name="src" location="src"/>
    <property name="build" location="build"/>

    <property name="lib" location="lib"/>
    <property name="dist" location="dist"/>
    <property name="packaging_tools" value="packaging_tools" />

    <property environment="env"/>

    <target name="clean">
        <mkdir dir="${build}"/>
        <delete includeEmptyDirs="true">
            <fileset dir="${build}" includes="**/*"/>
        </delete>

    </target>

    <target name="init">
        <!-- Create the time stamp -->
        <tstamp/>
        <!-- Create the build directory structure used by compile -->
        <mkdir dir="${build}"/>
        <mkdir dir="${dist}"/>
        <mkdir dir="${lib}"/>
    </target>

    <available file=".git" type="dir" property="git.present"/>

    <target name="git.commit" description="Store git revision in repository.commit" if="git.present">
        <exec executable="git" outputproperty="git.commit" failifexecutionfails="false" errorproperty="">
            <arg value="describe"/>
            <arg value="--always"/>
            <arg value="--dirty"/>
        </exec>
        <condition property="repository.commit" value="${git.commit}" else="git-commit-unavailable">
            <and>
                <isset property="git.commit"/>
                <length string="${git.commit}" trim="yes" length="0" when="greater"/>
            </and>
        </condition>
    </target>

    <target name="git.commit.absent" description="Fallback when git is not present" unless="git.present">
        <property name="repository.commit" value="git-commit-unavailable"/>
    </target>

    <target name="revision" depends="init,git.commit,git.commit.absent"
            description="Write revision.txt into the build directory">
        <copy file="revision.txt.template" tofile="${build}/revision.txt" overwrite="yes">
            <filterchain>
                <replacetokens>
                    <token key="repository.commit" value="${repository.commit}"/>
                </replacetokens>
            </filterchain>
        </copy>
    </target>

    <target name="compile" depends="revision">
        <!-- Compile the java code from ${src} into ${build} -->
        <javac release="11" srcdir="${src}" destdir="${build}"
            classpath="${lib}/jebl.jar:${lib}/jam.jar:${lib}/batik-awt-util.jar:${lib}/batik-dom.jar:${lib}/batik-ext.jar:${lib}/batik-svggen.jar:${lib}/batik-util.jar:${lib}/batik-xml.jar:${lib}/xml-apis.jar:${lib}/iText.jar:${lib}/VAqua12.jar:${lib}/flatlaf-3.2.5.jar:${lib}/jwt/servlet-api-2.5-6.0.2.jar:${lib}/jwt/jwt-3.1.7pre1.jar">
            <include name="figtree/**"/>
        </javac>
        <copy todir="${build}" verbose="true">
            <fileset dir="${src}" includes="figtree/**/*.png,figtree/**/*.gif"/>
        </copy>
    </target>

    <target name="dist" depends="compile" description="generate the distribution">
        <!-- Create the distribution directory -->
        <mkdir dir="${dist}"/>

        <jar jarfile="${dist}/figtree.jar">
            <zipgroupfileset dir="${lib}" includes="jebl.jar"/>
            <zipgroupfileset dir="${lib}" includes="jam.jar"/>
            <zipgroupfileset dir="${lib}" includes="batik*.jar"/>
            <zipgroupfileset dir="${lib}" includes="xml-apis.jar"/>
            <zipgroupfileset dir="${lib}" includes="iText.jar"/>
            <zipgroupfileset dir="${lib}" includes="w-3.2.5.jar"/>
            <fileset dir="${build}" includes="**/*.class,**/*.properties,**/*.png,**/*.gif,revision.txt"/>
            <manifest>
                <attribute name="Built-By" value="${user.name}"/>
                <attribute name="Main-Class" value="figtree.application.FigTreeApplication"/>
            </manifest>
        </jar>

        <jar jarfile="${dist}/figtreepanel.jar">
            <fileset dir="${build}"
                     includes="figtree/panel/**/*.class,figtree/treeviewer/**/*.class,**/*.properties,**/*.png,**/*.gif"
                    />

            <manifest>
                <attribute name="Built-By" value="${user.name}"/>
            </manifest>
        </jar>

        <jar jarfile="${dist}/figtree-pdf.jar">
            <fileset dir="${build}"
                     includes="figtree/panel/**/*.class,figtree/treeviewer/**/*.class,**/*.properties,**/*.png,**/*.gif"
                    />

            <manifest>
                <attribute name="Built-By" value="${user.name}"/>
            </manifest>
        </jar>

    </target>

</project>
